datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Define your models here
model Post {
  id    Int     @id @default(autoincrement())
  title String
  body  String
}
